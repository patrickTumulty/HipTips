<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:HipTips"
             x:Class="HipTips.ShiftPage">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" HorizontalOptions="FillAndExpand" Padding="10, 20">
            <Entry Placeholder="Tips" HorizontalOptions="FillAndExpand"
                   x:Name="ent_Tips"/>
            <Entry Placeholder="Hours" HorizontalOptions="FillAndExpand"
                   x:Name="ent_Hours"/>
            <Button Text="Save Shift" Clicked="Save_Shift" 
                    HorizontalOptions="FillAndExpand"
                    BackgroundColor="#b4daf1"
                    FontSize="Large"
                    TextColor="White"
                    />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
